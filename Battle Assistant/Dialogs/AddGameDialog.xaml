<ContentDialog
    x:Class="Battle_Assistant.Dialogs.AddGameDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Battle_Assistant.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Add Game"
    PrimaryButtonText="Add Game"
    CloseButtonText="Cancel"
    PrimaryButtonClick="AddGame_Click"
    IsPrimaryButtonEnabled="False">

    <StackPanel Spacing="24">
        <InfoBar x:Name="DialogInfoBar" 
                 IsOpen="False"/>
        
        <!-- Game Directory Input -->
        <StackPanel Spacing="8">
            <TextBlock>Game Directory</TextBlock>
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBox PlaceholderText="The folder that contains its Game Files folder" Text="{Binding Game.GameDir, Mode=OneWay}" Width="380"/>
                <Button Content="Select Folder" Click="SelectGameDir_Click" Width="105"/>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</ContentDialog>
