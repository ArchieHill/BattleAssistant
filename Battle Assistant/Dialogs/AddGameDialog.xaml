<ContentDialog
    x:Class="Battle_Assistant.Dialogs.AddGameDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Battle_Assistant.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Add Game"
    PrimaryButtonText="Add Game"
    CloseButtonText="Cancel"
    PrimaryButtonClick="AddGame_Click"
    IsPrimaryButtonEnabled="False">

    <StackPanel>
        <TextBlock>Game Files Directory</TextBlock>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Game.GameDir.Path, Mode=OneWay}"/>
            <Button Content="Select Folder" Click="SelectGameDir_Click"/>
        </StackPanel>

        <TextBlock>Game Icon (Optional)</TextBlock>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Game.GameIcon.Path, Mode=OneWay}"/>
            <Button Content="Select File" Click="SelectIconFile_Click"/>
        </StackPanel>
    </StackPanel>
</ContentDialog>
