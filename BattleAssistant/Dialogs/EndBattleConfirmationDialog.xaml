<ContentDialog
    x:Class="BattleAssistant.Dialogs.EndBattleConfirmationDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BattleAssistant.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="End Battle Confirmation"
    PrimaryButtonText="End Battle"
    CloseButtonText="Cancel"
    PrimaryButtonCommand="{Binding EndBattleCommand}">

    <StackPanel Style="{StaticResource ListStackPanel}">
        <ToggleSwitch Header="Clean Up Folders" IsOn="{Binding CleanUpFolders, Mode=TwoWay}" ToolTipService.ToolTip="Deletes all the battle files in incoming, outgoing and shared folders."/>
        <TextBlock>Are you sure you want to end the battle?</TextBlock>
    </StackPanel>
</ContentDialog>
